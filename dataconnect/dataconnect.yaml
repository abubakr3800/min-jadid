specVersion: "v1"
serviceId: "min"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "min-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
